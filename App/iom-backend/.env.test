# === .env.test ===

# Server
PORT=4001

# Test Database (separate from dev/prod)
MONGO_URI=mongodb://localhost:27017/iom-test

# JWT Secret (test key)
JWT_SECRET=test_secret_key

# OctoPrint (mock endpoints - should NOT point to real printers)
OCTOPRINT_BASE_URL=http://localhost:8080
OCTOPRINT_API_KEY=test-octoprint-api-key
OCTOPRINT_MULTICOLOR_PATH=/mock-printer-multicolor
OCTOPRINT_DIRECT_PATH=/mock-printer-direct
WEBCAM_DIRECT=http://localhost:8080/test-webcam
WEBCAM_MULTICOLOR=http://localhost:8080/test-webcam

# SMTP (mock or throwaway testing account)
SMTP_HOST=smtp.test.com
SMTP_PORT=587
SMTP_USER=test@example.com
SMTP_PASS=test_password

# Frontend test placeholder
BASE_WEB_URL=http://localhost:3000
